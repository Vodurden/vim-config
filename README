Installation:

    git clone git://github.com/nelstrom/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update

Dependencies:
	clang_complete:
		Python Support
		libclang accessable from /usr/lib (modify g:clang_library_path if different) assumes clang > 2.8
	command-t:
		Ruby 1.8 (with devel if compiling)
	ProtoDef:
		astyle (Runs a style after generating)
